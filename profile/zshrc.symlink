# zshrc runs for interactive shells after zprofile
shell="zsh"

if [[ -o login ]]; then
  export isloginshell=1
else
  export isloginshell=0
fi

source $HOME/dotfiles/profile/common.sh

for rcfile in $DOTFILES/*/rc_zsh.sh; do
	source $rcfile
done
